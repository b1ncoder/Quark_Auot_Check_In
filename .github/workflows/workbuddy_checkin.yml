name: WorkBuddy Daily Check-in

on:
  schedule:
    # Primary run: 09:00 Asia/Shanghai = 01:00 UTC
    - cron: '0 1 * * *'
    # Backup run: same day 21:00 Asia/Shanghai = 13:00 UTC
    # The API is idempotent, so this safely catches scheduler delays.
    - cron: '0 13 * * *'
  workflow_dispatch:

jobs:
  checkin:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'

      - name: Install dependencies
        run: python -m pip install --disable-pip-version-check requests

      - name: WorkBuddy check-in
        env:
          WORKBUDDY_TOKEN: ${{ secrets.WORKBUDDY_TOKEN }}
        run: python workbuddy_checkin.py
